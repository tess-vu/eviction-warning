name: eviction-warning
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13.12
  - gtk3
  - pip
  - pip:
    - fairlearn>=0.10
    - scikit-learn>=1.4
    - pandas>=2.0
    - numpy>=1.26
    - pytest>=8.0
    - weasyprint>=61
    - jinja2>=3.1
    - pillow>=10
